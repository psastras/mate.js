general:
  branches:
    only:
      - master # list of branches to build
      - /feature-.*/ # or regexes
machine:
  node:
    version: 6.1.0
deployment:
  pages:
    branch: master
    commands:
      - npm run doc
      - sh ci/github-page.sh